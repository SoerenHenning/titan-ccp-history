// Apply the java-library plugin to add support for Java Library
apply plugin: 'java-library' //TODO not a library
apply plugin: 'eclipse'

repositories {
    jcenter()
    maven {
    	url "https://oss.sonatype.org/content/repositories/snapshots/"
    }
}

dependencies {
    // This dependencies is exported to consumers, that is to say found on their compile classpath.
    api 'net.kieker-monitoring:kieker:1.14-SNAPSHOT'
    api 'net.sourceforge.teetime:teetime:3.0-SNAPSHOT'

    // This dependencies are used internally, and not exposed to consumers on their own compile classpath.
    implementation 'org.apache.kafka:kafka-clients:1.0.0'
    implementation 'com.google.guava:guava:20.0'
    
    implementation 'com.google.code.gson:gson:2.8.2'

    // Use JUnit test framework
    testImplementation 'junit:junit:4.12'
}

eclipse {
    classpath {
       downloadSources=true
       downloadJavadoc=true
    }
}